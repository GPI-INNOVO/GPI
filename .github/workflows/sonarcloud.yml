name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: npm ci
      - uses: SonarSource/sonarcloud-github-action@v2
        with:
          projectKey: gpi-innovo
          organization: GPI-Requinoa
          token: cc46718732008f2f74fbbc97e476f3db5270cc35